{
  "name": "sticks-golf",
  "private": true,
  "scripts": {
    "cap:prepare": "rm -rf www && mkdir www && cp index.html manifest.json sw.js icon-192.png icon-512.png www/ && cp -r fonts www/",
    "cap:sync": "npm run cap:prepare && LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 npx cap sync",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-react": "^7.28.5",
    "@capacitor/cli": "^6.0.0"
  }
}
